# MediaBox AI V2 - Port Configuration
# These ports avoid conflicts with V1 (8080, 8123, 5900, 6080)

# Flask Dashboard API (V1 uses 8080)
PORT=8081

# Voice Control API (V1 uses 8081, so we'll use 8082)
VOICE_PORT=8082

# Home Assistant (V1 uses 8123, V2 doesn't use HA yet)
# HASS_PORT=8124

# Environment
FLASK_ENV=development
FLASK_DEBUG=1

# Home Assistant Configuration (if needed)
HASS_USERNAME=threefingers
HASS_PASSWORD=threefingers
HASS_SERVER=http://localhost:8123

# Timezone
TZ=America/New_York

